# Undefined Surfaces

The current P0 inventory contains only `home` and `detail`, with loading and error rendered as states rather than additional screens. No account, payment, favorites, settings, notification, editing, or private-content surface is implied by the approved controls. If later research introduces any such entry point, it must first be classified as a contracted surface, overlay, dialog, background operation, or explicitly excluded current behavior before design expansion.
